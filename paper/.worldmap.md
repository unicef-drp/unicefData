# Worldmap — unicefData paper

Purpose: anchor sources and scope to avoid drift/hallucination while editing the Stata Journal manuscript.

## Core files
- Main manuscript: unicefData_documentation_draft.tex
- Improvement guide: IMPROVEMENT_PLAN.md
- Bibliography: references.bib
- Related YAML paper (for style/examples): ../../yaml/paper/submission/latex/main-v3.tex
- Supplemental references collection: ../../others/StataCI-research/05_references

## Canonical facts
- Target format: Stata Journal (statapress + sj_clean/stata packages)
- Ecosystem: Stata command `unicefdata` with R (`get_unicef`) and Python (`unicef_api`) sibling clients
- Metadata store: YAML files (_unicefdata_indicators.yaml, _unicefdata_dataflows.yaml, _unicefdata_codelists.yaml, _unicefdata_countries.yaml, _unicefdata_regions.yaml)
- Key features: discovery commands (categories, flows, indicators, info, search); data retrieval with filters (countries, year, sex, age, wealth, residence, maternal_edu); output formats (long, wide, wide_indicators); sync utilities (unicefdata_sync, unicefdata_xmltoyaml)

## Style/formatting constraints
- Non-floating tables (use captionof); captions footnotesize; footnotes left-aligned (footmisc)
- Keep ASCII; follow SemVer/version headers if editing ado/sthlp
- Prefer compound quotes for SMCL in Stata; simple quotes for word extraction (see .github/copilot-stata-instructions.md)

## Pending vigilance
- Avoid inventing dataflow names/codes beyond those listed in manuscript
- Cite only entries present in references.bib; new citations must be added there first
- Example outputs should remain representative, not exact/hypothetical statistics

## Update cadence
- When adding sections/examples, align with IMPROVEMENT_PLAN.md phases and keep todo list in sync (manage_todo_list tool)
